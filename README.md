# Todo-Application-Backend
This repository contains the backend implementation of a Todo application built using Spring Boot and MySQL. It exposes a set of RESTful API endpoints that allow clients to perform full CRUD operations on tasks.
